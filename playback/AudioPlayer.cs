using System;
using System.Collections.Generic;
using System.Text;

namespace MP3Decoder.playback
{
    internal class AudioPlayer
    {
        public static void run()
        {
            // select decoder type, for now only have a 'basic' option

            // pull .mp3 files from 'data' directory and pass to selected decoder
        }
    }
}
